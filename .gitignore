__pycache__/
*.pyc
*.pyo
.env
.env.staging.local
.env.production.local
.env.local
.pytest_cache/
*.egg-info/
dist/
build/

.coverage
htmlcov/
logs/*.jsonl

# Phase 147: 런타임 데이터 파일
# 셀러 마켓 자격증명(암호문) — 레포에 들어갈 것이 아니다.
#   F32-3·4에서 테스트가 실제로 이 자리에 파일을 써 커밋됐다(가짜 값이었지만
#   자격 파일이 레포에 들어가는 길이 열려 있었다는 뜻이다).
data/market_credentials/

data/job_queue.jsonl
data/job_dead_letter.jsonl
data/push_subscriptions.jsonl
data/omni_sync_log.jsonl
# Phase 149: 런타임 캐시
data/analytics_cache.jsonl

# 로컬 하네스용 node 의존성(jsdom) — 커밋 대상 아님(CI 게이트는 Playwright).
node_modules/
package.json
package-lock.json
