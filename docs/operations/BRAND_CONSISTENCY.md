# BRAND_CONSISTENCY (Phase 152)

- 표준 브랜드명: `BRAND_NAME` (기본 `Proxy Commerce`)
- 기본 템플릿 title 포맷: `{{ page_title }} | {{ brand_name }}`
- OpenGraph:
  - `og:site_name = {{ brand_name }}`
  - `og:title = {{ brand_name }}`

Google OAuth 동의 화면 앱 이름도 동일하게 맞춰야 합니다.
- 콘솔: https://console.cloud.google.com/auth/branding
