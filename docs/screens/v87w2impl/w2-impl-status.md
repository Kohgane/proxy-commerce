# v87-W2-impl · 주문 상태 계층색 + 비색 단서 (A안 구현)

제안서 #592 §7(선택지 A) 확정 구현. 부트스트랩 무순서 색 → **거친 계층색(4) + 비-색 단서**(단계 도트·아이콘). 색약·흑백에서도 정보 손실 0.

## 매핑 (신설 공통 컴포넌트 `_status_badge.html` + `.pc-lc`)
| 계층 | 상태 | 색(계층) | 비-색 단서 |
|---|---|---|---|
| 진행 | 신규접수·결제완료·상품준비중·배송중 | **금 1색**(`--gold`/`--gold-ink`) | **채운 도트 수 1·2·3·4** (`●○○○`→`●●●●`) + 라벨 |
| 완료 | 배송완료 | 청록(`--teal`) | `bi-check-circle` + 라벨 |
| 종결-중립 | 취소 | 먹 뮤트(`--ink-soft`) | `bi-slash-circle` + 라벨 |
| 종결-되돌림 | 반품·교환·환불 | **브론즈**(`--st-return`, ≠danger 적색) | `bi-arrow-counterclockwise` + 라벨 |

- **마켓 색코딩 제거** → 전부 `pc-badge-muted`(마켓명이 항상 구분자, 색은 중복이었음). 정보 손실 0.
- **반품 계열 danger 분리**: 되돌림 톤(`--st-return` 브론즈)은 시스템 오류(danger 적색)와 색·아이콘 둘 다 다름 → 색약에서도 "정상 되돌림 vs 오류" 구분.

## 그레이스케일 판별 계약 (핵심)
`test_v87_w2_impl_status`(8): 색을 지운 뒤(같은 계층색끼리) 비-색 단서만으로 9상태 전부 구분됨을 못박음.
- 진행 4단계 = 같은 금색, **도트 수 1/2/3/4로 단조 구분**(흑백 생존).
- 9상태 비-색 서명(도트수·아이콘·라벨) 전부 유일 → 흑백에서 뭉갬 0.
- `.pc-lc-return`이 `--danger` 아닌 `--st-return` 사용(오류 충돌 분리) CSS 계약.
- orders.html: `badge bg-*`·`status_colors` 잔재 0, 마켓=뮤트.

## 정직·불변
- 라벨 전부 보존(정밀 구분은 항상 라벨) · 상태 변경은 서버 리로드로 매크로 재렌더(JS 색뱃지 주입 0).
- gogabridj 토큰 단일소스(하드코딩 hex 0 — `--st-return`만 :root 신설, 이모지 0).

캡처: `w2-impl-status.png` — 좌 BEFORE(부트스트랩 무순서 색) / 중 AFTER(계층색+도트/아이콘) / **우 AFTER 그레이스케일(색약 재검증 — 도트·아이콘 생존)**. 9상태 mock 실렌더.
