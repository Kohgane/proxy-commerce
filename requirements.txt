requests==2.32.3
python-frontmatter==1.0.1
PyYAML==6.0.2
python-dotenv==1.0.1
gspread==6.1.2
oauth2client==4.1.3
google-auth==2.34.0
google-auth-oauthlib==1.2.1
Flask==3.0.3
Flask-Limiter==4.1.1
Flask-Cors==6.0.2
cloudinary==1.41.0
Babel==2.15.0
gunicorn==25.1.0
beautifulsoup4>=4.12
lxml>=5.0
fake-useragent>=1.4
pydantic>=2.0
APScheduler>=3.10
bcrypt>=3.0,<5.0
cryptography>=42.0,<49
webauthn>=2.0,<3  # v40-D 패스키 — cryptography<49 호환(2.x 라인)
psycopg[binary,pool]>=3.1,<4  # v49 STEP2 pool = 상시 커넥션 풀(PG_PERSISTENT_POOL=1일 때만)
openpyxl>=3.1,<4  # v47 STEP5 엑셀 벌크 내보내기/업로드
Pillow>=10.0,<14  # 카나리 3차: 네이버 미허용 WebP → JPEG 실변환(image_norm) + 이미지 파이프라인. 이전엔 requirements 미기재라 프로덕션 이미지에 없었다(조용한 무력화).

# D1 이미지 번역 — 텐센트 TMT `ImageTranslateLLM`.
#   **제품 단위 패키지**를 쓴다(전체 SDK가 아니라). 이미지에 안 쓰는 제품 모듈이 딸려 오지 않게.
#   common은 tmt의 의존성이지만 버전을 눈에 보이게 두려고 함께 적는다.
tencentcloud-sdk-python-tmt>=3.1.129,<4
tencentcloud-sdk-python-common>=3.1.175,<4

# D3-3 이미지 번역 렌더 — 박스 inpaint + 한국어 폭맞춤 렌더(3단계).
#   headless: 서버에 GUI(libGL·X11)가 없다. `opencv-python`(비-headless)을 넣으면
#   부팅이 libGL.so.1 부재로 죽는다 — headless가 유일한 선택이다.
#   Pillow만으로는 골든 샘플 5축 C(배경 복원)·E(자연스러움)를 못 넘긴다(오너 판단 2026-09-20).
#   ★ 이 줄이 들어오면서 `image_pipeline`의 워터마크 경로가 **처음으로 살 수 있게** 된다.
#     검토 없이 살아나지 않도록 `IMAGE_INPAINT_ENABLED` 기본값을 0으로 내려 뒀다(F43).
#   실측 설치 용량: opencv-python-headless 162MB · numpy 73MB (압축 wheel 62MB·17MB).
opencv-python-headless>=4.10,<5
numpy>=1.26,<3
