name: CD – Staging

on:
  push:
    branches:
      - main

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install -r requirements-dev.txt

      - name: Run tests
        run: pytest --tb=short -q

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          build-args: |
            APP_ENV=staging
          tags: |
            ghcr.io/kohgane/proxy-commerce:staging-${{ github.sha }}
            ghcr.io/kohgane/proxy-commerce:staging-latest

      - name: Deploy to Render (staging)
        run: |
          if [ -z "$RENDER_DEPLOY_HOOK_STAGING" ]; then
            echo "RENDER_DEPLOY_HOOK_STAGING not set – skipping deploy"
            exit 0
          fi
          curl -fsSL -X POST "$RENDER_DEPLOY_HOOK_STAGING"
        env:
          RENDER_DEPLOY_HOOK_STAGING: ${{ secrets.RENDER_DEPLOY_HOOK_STAGING }}

      - name: Post-deploy healthcheck
        run: |
          if [ -z "$STAGING_APP_URL" ]; then
            echo "STAGING_APP_URL not set – skipping healthcheck"
            exit 0
          fi
          # Wait for the service to become reachable after the deploy hook fires
          sleep 30
          python scripts/post_deploy_check.py \
            --url "$STAGING_APP_URL" \
            --env staging \
            --retries 5 \
            --interval 15
        env:
          STAGING_APP_URL: ${{ secrets.STAGING_APP_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.STAGING_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.STAGING_TELEGRAM_CHAT_ID }}
          APP_VERSION: staging-${{ github.sha }}

      - name: Notify success
        if: success()
        env:
          STAGING_APP_URL: ${{ secrets.STAGING_APP_URL }}
          TELEGRAM_BOT_TOKEN: ${{ secrets.STAGING_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.STAGING_TELEGRAM_CHAT_ID }}
          APP_VERSION: staging-${{ github.sha }}
        run: |
          if [ -z "$TELEGRAM_BOT_TOKEN" ]; then
            echo "STAGING_TELEGRAM_BOT_TOKEN not set – skipping notification"
            exit 0
          fi
          python scripts/post_deploy_check.py \
            --url "$STAGING_APP_URL" \
            --env staging \
            --notify-only

      - name: Notify failure
        if: failure()
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.STAGING_TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.STAGING_TELEGRAM_CHAT_ID }}
          APP_VERSION: staging-${{ github.sha }}
        run: |
          python -c "
          import os, requests
          bot = os.getenv('TELEGRAM_BOT_TOKEN')
          chat = os.getenv('TELEGRAM_CHAT_ID')
          version = os.getenv('APP_VERSION', 'unknown')
          if bot and chat:
              requests.post(
                  f'https://api.telegram.org/bot{bot}/sendMessage',
                  json={'chat_id': chat, 'text': f'❌ [staging] 배포 실패 - {version}'},
                  timeout=15
              )
          "
